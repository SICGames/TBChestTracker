<Window x:Class="TBChestTracker.ValidateChestDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TBChestTracker"
        mc:Ignorable="d"
        Title="Validate Chest Data Integrity" Height="400" Width="600" Topmost="True" Closing="Window_Closing" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" Loaded="Window_Loaded" Unloaded="Window_Unloaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding StatusMessage}" FontWeight="Bold" FontSize="18"/>
            <ProgressBar IsIndeterminate="True" Height="10" Margin="0,10"/>
        </StackPanel>
        <Grid Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center">
            
        </Grid>
    </Grid>
</Window>
